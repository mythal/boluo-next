import { Page } from '../../helper/layout';
import { getLayout } from '../../components/DesignLayout';

const Design: Page = () => {
  return <p>This is the place to design and tweak UI components.</p>;
};

Design.getLayout = getLayout;

export default Design;
